# UniHub Retail v2.0.1

Release status: prepared, not yet tagged or deployed
Code candidate: `6dafc72899dcf9e500e675d68973aec21e495880`
Pre-rollout production SHA: `d08add17147d9f24b23642dbb1a0084708ff9307`

`v2.0.1` is a correctness and security hotfix. It does not move or replace the
historical `v2.0.0` tag and does not add product functionality.

## Critical risk remediation

### Import and master-data safety

- A sales file can no longer activate or deactivate stores by omission.
- Store closure/reactivation is a separate CAS-protected, reasoned and audited
  operation tied to the OIDC subject.
- Monetary values, quantities, required identifiers, duplicate headers/rows and
  contradictory store metadata fail before import writes.
- Every snapshot persists a privacy-safe coverage/diff report before promotion.
- Full, 90%, single-company, conflict, duplicate, idempotent retry and concurrent
  same-month cases are covered by isolated PostgreSQL tests.

Migration: `026_import_master_data_safety.sql`
SHA-256: `30063b80755ed86db05cd87c9b130fa632f8ea435df7ddf8c26ce0a38d2999b7`

### Grile check and target synchronization

- `/api/grile/run` and its worker job are strictly read-only.
- Diff and privileged synchronization are separate operations and reservations.
- Sync requires the dedicated OIDC group, CSRF, rate limiting and an audit row
  keyed by stable OIDC `sub`, not email.
- Managed/unmanaged target boundaries and the full target-state hash prevent an
  apparently clean check from hiding mutations.

Migration: `027_grile_target_sync_audit.sql`
SHA-256: `85cfa465351184ded8cd1f0bbae8fe7a084703c92019ea9c7c6093c0d81864d2`

### Salary finalization, archive and reset

- Invalid numeric cells, missing agents/stores, unexpected registry entries,
  Google timeout/429/503 and incomplete coverage fail the operation.
- Partial workbooks and archives never receive or retain the official path.
- Persistent manifests record operation/month, expected and processed coverage,
  control totals, zero-error state, artifact SHA-256, `requested_by_sub`,
  `approved_by_sub` and a verifiable status/hash.
- Archive approval must still be the newest archive attempt when reset starts.
- Live reset requires an approved verified archive, complete source backups and
  per-store checkpoints; rollback is verified and an uncertain checkpoint blocks
  automatic retry.
- Legacy queued jobs fail or normalize safely without treating an email address
  as authorization.

Migration: `028_grile_monthly_fail_closed.sql`
SHA-256: `6bbf34dcb10d923812fb815eb6ef76ae806422f212e893e45bd16fbdb091c2aa`

### Pull-request runner isolation

- PR code runs only on GitHub-hosted `ubuntu-24.04` runners.
- The isolation job proves absence of production paths, the Tailscale interface
  and the production Docker identity/socket. Its direct, no-proxy TimesFM probe
  treats every HTTP response, including 401/403/404, as reachable and fails.
- The former persistent production-host runner was stopped and removed from the
  repository runner inventory.
- Exact private `@unihub/*` packages are vendored and integrity-checked, so CI
  neither assumes access to `127.0.0.1:4873` nor receives a Verdaccio token.
- GitHub actions used by CI/deploy are pinned to commit SHA.
- A successful `main` run produces an immutable source+`dist` archive with source
  SHA and SHA-256 manifest; PR runs never produce a deploy artifact.

## Isolated fixes

- Sensitive API, salary, P&L, photo and export responses receive
  `Cache-Control: private, no-store, max-age=0` plus CDN no-store controls.
- FastAPI docs, Redoc and OpenAPI JSON are disabled in production code.
- SPA fallback is limited to GET/HEAD HTML navigations outside server/API/auth
  namespaces and never masks missing assets.
- Target Calculator validates the complete batch before any row/revision write;
  one invalid code leaves the transaction unchanged.
- Session refresh uses token-owned compare-delete locks, a bounded owner window
  below the lease, bounded waiters and ciphertext compare-delete so concurrent
  requests cannot delete a successfully rotated session.

The public reverse proxy must still return 404 for `/metrics`, `/docs`, `/redoc`
and `/openapi.json` while the internal Prometheus scrape remains healthy. That is
a rollout gate, not a claim about the pre-release production deployment.

## Redesign verification

The “Ajustări” tranche between `aba3fa0` and live SHA `d08add1` was reviewed as
part of this release preparation. The P&L, responsive shell, management tables,
compact Hub and removal of the obsolete mobile hint were mapped commit-by-commit.
The final frontend gates passed typecheck, strict typecheck, lint, 228 unit tests,
build and sequential desktop/mobile/accessibility Playwright tests. No additional
cosmetic change was added to this hotfix.

## Verification evidence

- Local isolated backend stack: 1,250 passed, 9 skipped, 99% total coverage.
- Critical coverage: Grile monthly 98.23%, importer 95.98%, Target Calculator
  100%; every configured threshold passed.
- Mypy: 255 source files clean on the final patch stack.
- Pip audit, npm runtime audit, tracked-secret scan and Bandit passed. The Bandit
  baseline contains 17 Medium findings; the earlier value 111 was incorrect.
- PR merge-ref CI passed for #95, #96, #97, #98, #99 and #101.
- Final code `main` CI run `29444282142` passed runner isolation, backend,
  frontend, build, Playwright/accessibility and uploaded the verified release
  artifact for `6dafc72899dcf9e500e675d68973aec21e495880`.

## Read-only production reconciliation

June contained three stores absent from July. All three are currently inactive.
No store was reactivated and no business status was changed. The release does not
infer that these stores should reopen; any change requires explicit business
approval through the audited activity operation.

The TimesFM endpoint `100.74.73.114` is a Tailscale peer. Overlay transport is
encrypted; the remaining open risk is peer/data governance, ACL ownership,
payload minimization, key rotation and processor retention, not plaintext
transport over the public internet.

## Deployment and rollback gates

Do not tag or publish this release until all items below are evidenced on the
exact final `main` SHA:

1. required environment approval is enforced;
2. PostgreSQL and Grile outputs are backed up and restorable;
3. migrations 026-028 pass checksum verification and run through the one-shot
   migration unit;
4. the root-owned deploy entrypoint is provisioned, reviewed and limited to the
   dedicated deploy runner;
5. the exact CI artifact is downloaded and its SHA-256/source SHA are verified;
6. backend and worker restart cleanly and local/public liveness/readiness pass;
7. public metrics/docs routes are denied while internal Prometheus stays healthy;
8. import, Grile read-only/sync, salary finalization/archive/reset, P&L, Visits,
   desktop and mobile acceptance pass without production business writes;
9. rollback to the backed-up application/DB/output state is demonstrated.

The deploy workflow is fail-closed because the current private-repository plan
does not expose required environment reviewers, and its expected root-owned
entrypoint is not provisioned in the pre-rollout state.
`PRODUCTION_DEPLOY_APPROVALS_ENFORCED` must not be enabled until both controls are
genuinely available and reviewed. A manual deploy is not an accepted substitute.

## Known remaining work

The reconciled audit keeps all non-remediated findings explicitly open, including
Google identity separation/retention, durable upload spool/outbox, campaign
fail-closed calculations, export scalability, organizational read scope, visits
pagination/sync, full-stack E2E, global coverage, service-user isolation and
multi-host availability. These belong to later compatible releases and are not
silently reclassified as complete by this hotfix.
