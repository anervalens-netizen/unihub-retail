# UR-PR153-READY-20260814 — PR #153 readiness, merge, deploy

Status: ACTIVE
Overall outcome: UNVERIFIED
Owner: primary agent
Parent plan: UR-CLOSE-20260812 (this is a focused sub-stream under its
            Release B delivery section)
Created: 2026-08-14T13:55:00+03:00
Updated: 2026-08-14T13:55:00+03:00

## Objective

Take PR #153 "Release B: close Retail sales, promo, outbox and Grile
incidents" from "OPEN, conflict, no green CI, three unresolved comments"
to "merged on main, signed artifact, deployed, auditor PASS".

Non-negotiable safety fences:

- No salary export, salary apply, finance apply, Target finalization, Grile
  reset, Grile destructive.
- Outbox, promo, sales promotion, campaign/grile lineage, and Grile V2
  outbox delivery are within scope; everything else is read-only.
- Frontend controller-boundary refactor is in scope but earns no merge if any
  boundary breaks an existing user flow.
- Authentik, Caddy, NAS sync, Backup, DR, and shared gateways are OUT.
- Force-push on `codex/retail-close-b-lean` allowed only after local
  verifier green and `--force-with-lease` is mandatory; documented as an
  evidence point.

## Sources

- PR #153: https://github.com/anervalens-netizen/unihub-retail/pull/153
- PR review comments (3): on commit ids `b78ba643`, `9e86f19a`, `b5b43807`
- Parent plan: UR-CLOSE-20260812 (sections D formal delivery, E production proof)
- Quality gates: AGENTS.md + `.agent/PLANS.md` + `scripts/check_complexity_ratchet.py` +
  `scripts/check_python_complexity_contract.py`
- ADR-006: docs/adr/006-verified-runtime-delivery.md

## Baseline

- Repository root: `/opt/Mobiup/unihub-retail`
- Working tree: clean (worktree at `/opt/Mobiup/unihub-retail-pr153-readonly`,
  detached HEAD, no untracked files as of plan creation)
- Branch: `codex/retail-close-b-lean`
- PR tip SHA: `62ae9b2c09f1a43a32c1709050ae33ffd3da4956`
- main HEAD SHA (upstream): `bc25bc4b61a5adab7deae83fde4210ad51a33b66`
- Divergence: PR is 61 ahead, 3 behind main
- PR mergeable state: `dirty` (CONFLICTING at GitHub API level)
- Last GH Actions CI on the PR branch: failure `31777321597` at 06:44Z
  on commit `586bdbc3` (now 11 commits behind the tip)
- Codex code-review auto-bot: quota exhausted (06:28-09:50Z errors)
- Live runtime on `server`: backend `bc25bc4` since 13:36 EEST, all green

## Minimal code map

- `backend/tests/test_telemetry_privacy_contract.py:22` — CNP literal fixture
- `scripts/complexity-ratchet.json` — legacy allowlist with stale entries
- `scripts/check_complexity_ratchet.py` — gate enforcing the allowlist
- `backend/services/sales_generation_flow.py` — Bucharest cutoff (P2 fixed)
- `backend/services/campaign_reporting_worker.py` — pool connection reuse (P2 fixed)
- `backend/services/outbox_*.py`, `transactional_outbox.py`,
  `grile_outbox_delivery.py` — Release B core (untouched here, only verified)
- `backend/services/sales_generation.py`, `promo_*`, `grile_pilot_v2_*`,
  `campaign_reporting.py` — Release B core (verified only)

## Acceptance contract

| ID | Observable behavior | Must not change | Verification and expected result | Runtime proof | Status |
|---|---|---|---|---|---|
| AC-1 | No literal 13-digit CNP-shaped value anywhere in `backend/` source | Privacy boundary: no CNP in API, metrics, manifests, diffs | `grep -REn "CNP[ \\t]*=[ \\t]*[\\\"'][0-9]{13}[\\\"']\|cnp[ \\t]*=[ \\t]*[\\\"'][0-9]{13}[\\\"']" backend/` returns 0 rows | N/A (source review) | UNVERIFIED |
| AC-2 | `scripts/complexity-ratchet.json` no longer references files/functions that no longer exist | Existing legacy entries that are still oversized remain; only stale entries (file/function moved or shrunken) are removed/shrunk | `python scripts/check_complexity_ratchet.py --root backend` exits 0 | N/A | UNVERIFIED |
| AC-3 | Backend `pytest` exits 0 | All previously passing 1980 tests stay passing; new outbox tests stay green | `cd backend && .venv-test/bin/python -m pytest -q` — last line must be "N passed, ..skipped, ..failed = 0" | partial (DB-write tests gated by `READ_ONLY_DB_ALLOWED`, expected) | UNVERIFIED |
| AC-4 | `mypy --ignore-missing-imports --explicit-package-bases .` exits 0 on backend | 557 source files remain clean | `cd backend && .venv-test/bin/mypy --ignore-missing-imports --explicit-package-bases .` exits 0 with "Success: no issues found in N source files" | N/A | UNVERIFIED |
| AC-5 | `python scripts/check_complexity_ratchet.py --root backend --config scripts/complexity-ratchet.json` exits 0 | (covered by AC-2) | same command, exit 0 | N/A | UNVERIFIED |
| AC-6 | Branch `codex/retail-close-b-lean` advances from 62ae9b2c with exactly 2 coherent commits (CNP fix + ratchet fix), no force-push damage | All 61 existing PR commits preserved by content | `git log --oneline 62ae9b2c..HEAD` shows 2 commits; each commit diff is small and reversible | N/A | UNVERIFIED |
| AC-7 | Branch state: PR #153 still "OPEN" but `mergeable_state` advances to "clean" or "unstable" after push | (Cannot guarantee clean if main commits conflict with PR; mirror-evaluated via local 3-way merge) | `gh pr view 153 --json mergeable_state` after push shows `clean` or `unstable` (NOT `dirty`) | N/A | UNVERIFIED |
| AC-8 | Independent auditor returns `VERDICT: PASS` against AC-1..AC-7 | Auditor has no write authority; verdict format strict | auditor contract from skill (SKILL.md §6) | N/A | UNVERIFIED |
| AC-9 | GH Actions `CI` workflow completes successfully on the post-push PR tip | Re-runs `backend-check` including tracked-secret regression scan, bandit waiver, mypy, complexity, architecture-boundary, shell-static, operational-config, promtool-cache, exact-SHA, backend tests, mutation gate, coverage gate | `gh pr checks 153 --repo anervalens-netizen/unihub-retail` reports all required checks `success` | N/A | UNVERIFIED |
| AC-10 | `gh pr view 153 --json mergeable` shows mergeable=true and `--merge` succeeds; main HEAD post-merge contains the post-fix SHA | No unrelated history regression | `git log main..origin/main --oneline` (or equivalent) post-merge; main SHA recorded | N/A | UNVERIFIED |
| AC-11 | `Deploy verified Retail artifact` workflow on main produces a signed artifact bundle accepted by the formal deployer | Per ADR-006: SOURCE_SHA, SHA256SUMS, manifest, SBOM/provenance, Sigstore identity | CI workflow run link; `SHA256SUMS`; deploy run accepts and succeeds | N/A | UNVERIFIED |
| AC-12 | Production runtime on `server` carries the post-merge SHA; backend `/health` 200, `/readyz` 200; import-worker log shows outbox/promo/grile lineage work running normally post-deploy | No regression on existing active work (uniworker visits_sync, erp-reconciliation, etc.) | `systemctl show unihub-backend` start timestamp + journal tail; `curl /health`; `journalctl -u unihub-import-worker --since "10 minutes ago"` | live probe on `server` | UNVERIFIED |

## Tasks and dependencies

| Task | Scope | Depends on | Owner | Attempts | State |
|---|---|---|---|---:|---|
| T1 | Write plan, baseline established | none | primary | 0 | DONE |
| T2 | CNP literal removed from test fixture | T1 | primary | 0 | READY |
| T3 | complexity-ratchet.json stale entries cleared; ratchet exits 0 | T1 | primary | 0 | READY |
| T4 | Local pytest + mypy + ratchet exit 0 on post-fix SHA | T2,T3 | primary | 0 | READY |
| T5 | Independent auditor verdict PASS for AC-1..AC-7 | T4 | auditor | 0 | READY |
| T6 | Push branch with --force-with-lease | T5 PASS | primary | 0 | READY |
| T7 | GH Actions CI all required jobs PASS | T6 | GH Actions | 0 | READY |
| T8 | Merge PR #153 (after codex-review quota if it recharges, otherwise manual decision) | T7 | primary | 0 | READY |
| T9 | ADR-006 deploy + read-only live probe | T8 | primary | 0 | READY |
| T10 | Update plan to PASS or PARTIAL; record evidence; move to completed if done | T9 | primary | 0 | READY |

## Progress and transitions

- 2026-08-14T13:55:00+03:00 — Baseline recorded; all criteria UNVERIFIED. Next:
  read-only state preserved; begin T2.

## Attempts, failures, and discoveries

(none yet)

## Decisions

1. **Two discrete commits, not one squash.** The CNP fix and the ratchet
   config fix are independent and revertible. Splitting keeps the patch
   surface auditable per task.
2. **No rebase before push.** The 3 commits on main not in the PR are import
   hotfixes, not PR content. If GitHub reports conflicts at merge time,
   rebase is preferred over merge commit (preserves linear history as the
   existing plan dictates).
3. **`--force-with-lease`, not `--force`.** The PR branch tip will change; we
   must not silently overwrite concurrent work.
4. **Auditor against AC-1..AC-7 only, pre-push.** Post-push AC-8..AC-12
   follow because production deploy gates the final verdict.

## Auditor verdicts

(pending T5)

## Evidence index

(pending — will record each as it lands)

## Integration, regression, and deployment

- Integrated diff: UNVERIFIED (T4)
- Global checks: UNVERIFIED (T4)
- Published artifact/SHA: UNVERIFIED (T11)
- Runtime health/logs/user flow: UNVERIFIED (T12)

## Risks and remaining work

- The Codex code-review auto-bot is rate-limited; the 3 review comments will
  NOT get re-reviewed after the fixes. Acceptance therefore rests on the
  independent auditor (T5) and on AC-1 being independently re-greppable.
- `1655_corrupted` environment markers in older CI runs may resurface on
  the new SHA; if a fresh gate fails on infra (Promtool cache download),
  the failure must be triaged once and not retried in-loop.
- Grile V2 21/21 pilot live sync is OUT OF SCOPE here (separate workstream
  from PR #153 cron enqueue). Plan does not close it.
- Salary export UI/authenticated Excel remains unverified. It is OUT of
  SCOPE here; do not claim it's verified.

## Next exact step

T2 — in the worktree at `/opt/Mobiup/unihub-retail-pr153-readonly`, edit
`backend/tests/test_telemetry_privacy_contract.py` to construct the CNP-shaped
fixture value at runtime instead of embedding a literal 13-digit value. Then
`git diff` to confirm 1 file changed, AC-1 satisfiable.

## Resume procedure

1. Read this plan + parent plan `UR-CLOSE-20260812.md`.
2. `cd /opt/Mobiup/unihub-retail-pr153-readonly && git status -sb && git log -1`
3. Verify worktree is on detached HEAD at SHA recorded in `Attempts`/`Progress`.
4. Continue from `Next exact step`; do not repeat a recorded failed approach.
5. All cross-host actions use the documented SSH key (batch mode). Production
   probes go through the worktree and the live server.
