# Generated contract profile: sales import worker.
DATABASE_URL=postgresql://unihub_import_worker:change_me@127.0.0.1:5432/unihub # pragma: allowlist secret
UNIHUB_DB_PROCESS_AUTHORITY=sales_import
RETAIL_WORKER_ROLE=imports
VALKEY_URL=redis://127.0.0.1:6379/0
# Prometheus worker metrics endpoint topology (observability/worker_metrics.py).
PROMETHEUS_DOCKER_GATEWAY=
PROMETHEUS_DOCKER_SUBNET=
# JSON dictionary of trusted salary import reviewer public keys (Ed25519, base64),
# consumed by salary_import_approval.load_trusted_reviewer_keys(). Required for
# any live salary apply; missing/invalid configuration blocks apply.
SALARY_APPROVAL_REVIEWER_PUBLIC_KEYS_JSON=
