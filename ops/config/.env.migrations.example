# Generated contract profile: immutable migration runner.
MIGRATION_DATABASE_URL=postgresql://unihub_migration_runner:change_me@127.0.0.1:5432/unihub # pragma: allowlist secret
UNIHUB_DB_PROCESS_AUTHORITY=migrate
UNIHUB_MIGRATION_MAINTENANCE_WINDOW=
# One-time provisioning control: exactly "1" activates migrations 040 and 041
# for the one-time schema-authority cutover in db/migration_runner.py. Leave
# empty for every subsequent run.
UNIHUB_DB_AUTHORITY_CUTOVER_BOOTSTRAP=
